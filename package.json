{
  "name": "gaming-catalog",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["scripts/backend", "frontend"],
  "scripts": {
    "dev": "npm run dev --workspace scripts/backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace scripts/backend && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace scripts/backend && npm run typecheck --workspace frontend"
  }
}
